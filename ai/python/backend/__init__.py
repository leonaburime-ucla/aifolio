"""Backend namespace package for staged migration from ``ai/`` to ``backend/``.

This package provides stable import aliases while modules are incrementally
moved to domain folders (`backend.agents`, `backend.server`, `backend.ml`).
"""

