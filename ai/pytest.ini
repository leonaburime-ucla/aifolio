[pytest]
markers =
    unit: Fast unit tests for pure helpers and small utilities.
    contract: API/handler contract tests for payload validation and response shape.
    smoke: Lightweight end-to-end checks with tiny execution budgets.
    slow_matrix: Expensive dataset x algorithm matrix tests (manual/nightly).
    perf: Performance regression checks for training/distillation latency.
