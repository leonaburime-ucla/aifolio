"""
Framework-specific ML module adapters.

Each framework package exposes:
- `handlers`: request/response-oriented entrypoints for API transport
- `trainer`: runtime training/distillation/prediction entrypoints
"""
