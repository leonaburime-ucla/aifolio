# Market Data Feature (Orc-BASH)

Purpose: fetch + normalize market data for charts and cards.

- __specs__: versioned specs
- api: CoinGecko adapters
- logic: normalization + transforms
- state/zustand: cached data
- hooks: data hooks
- views: price cards, tables
