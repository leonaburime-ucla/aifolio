# ml-model-ui

UI-focused model visualization feature for ML training pages.

- Owns the "Show Model" modal and React Flow graph rendering.
- Exposes pure model-preview builders and a lightweight React hook.
- Consumed by:
  - `src/app/ml/pytorch/pytorch.tsx`
  - `src/app/ml/tensorflow/tensorflow.tsx`
