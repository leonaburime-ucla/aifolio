# AI Feature (Orc-BASH)

Purpose: chat sidebar, intent routing, tool orchestration.

- __specs__: versioned specs
- api: tool adapters, LLM clients
- logic: intent parsing, chart spec planning
- orchestrators: flow wiring
- state/zustand: chat state
- hooks: UI hooks
- views: sidebar UI
